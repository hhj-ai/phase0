# CPU side = wheelhouse builder target (cp310 manylinux) WITHOUT torch.
# Keep this list small and stable; GPU torch will be installed separately in requirements.gpu.txt.

numpy
packaging
pillow
tqdm
requests
pyyaml
regex
safetensors
tokenizers
transformers
huggingface_hub
datasets
pycocotools
qwen-vl-utils==0.0.14
scikit-learn==1.5.2
