# CPU-side (downloader) requirements for wheelhouse (cp310 manylinux)
# NOTE: 不包含 torch；torch 在 requirements.gpu.txt 里。
numpy
pillow
requests
tqdm
datasets==3.2.0
fsspec[http]<=2024.9.0
pycocotools==2.0.7
scikit-learn==1.5.2
qwen-vl-utils==0.0.14
transformers
