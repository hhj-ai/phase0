# Phase0 minimal deps (torch/cuda is provided by system-site-packages; do NOT pip install torch here)
# Target: Python 3.10 (Transformers 5.2.0 requires >=3.10)

numpy==1.26.4
pillow==10.4.0
opencv-python-headless==4.10.0.84
tqdm==4.66.5

# Qwen3-VL support lives in modern Transformers
transformers==5.2.0
accelerate==1.12.0

# sdist-only on PyPI (cpu.sh will download sdist and build a wheel into wheelhouse)
qwen-vl-utils==0.0.14

pycocotools==2.0.7
