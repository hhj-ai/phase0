# Minimal deps (no torch pin) for Phase0
numpy
pandas
Pillow
tqdm
pycocotools
matplotlib
# Most clusters already have torch/transformers; keep these unpinned to avoid fighting your environment.
transformers
accelerate
qwen-vl-utils
